import 'ptime';
import 'pjson';

d = dictionary([['h', 'e'], ['l', 'l'], ['o', '.']]);
js = pjson.dumps(d);
print(js);
print(pjson.loads('[1,2,3]').reversed());

while (true) {
    print(ptime.asctime(ptime.localtime()));
    sleep(7);
}